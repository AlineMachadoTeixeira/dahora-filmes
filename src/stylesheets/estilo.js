import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
  container: {
    backgroundColor: "#d3d7d7",
    flex: 1,
  },

  viewLogo: {
    backgroundColor: "#174b55",
    flex: 0.6,
  },
});

export default estilos;
